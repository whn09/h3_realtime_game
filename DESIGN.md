# 实时生成式开放世界互动影像

> 基于 MiniMax-H3 视频生成 + Bedrock LLM 的浏览器端互动叙事引擎。
> 用户设定世界观 → 引擎实时生成连续的影像段落 → 每段结束由用户选择剧情走向 → 无限延伸。

设计文档版本 v0.2 · 2026-09-21（v0.1 写于实现之前，本版按实测与实际落地改写）

**v0.2 改了什么 —— 十件被实测推翻的事**

| # | v0.1 怎么写的 | 实际是什么 | 影响 |
|---|---|---|---|
| 1 | 可用窗口 19.3s（15s 影像 + 4s 决断） | **14.375s**，且决断时刻**压在片尾最后 4s 上**、默认无倒计时 | 缺口不是 1.5s，是 p50 2.1s / p95 8.3s |
| 2 | PromptIR 8.1s 是最大的墙 | PromptIR 已降到 **4.6s**；**Director 才是最大的 LLM 项（9.6s）** | 2.3 节的杠杆排序整个反了 |
| 3 | 硬切约占 1/6 拍 | 实测 **65%**（cut 48 + timeskip 5，共 81 拍） | 图像成本被低估约 4 倍 |
| 4 | 开场约 21s | 实测 **约 110s**，其中 Worldsmith 一次调用 89.7s（首 token 就等 52.4s） | 开场仪式的设计量严重不足 |
| 5 | 关键帧用 Nova Canvas | **SD3.5 Large**（us-west-2），约 $0.08/张 | 成本结构一度整个翻过来（图像 > 文本），又被第 10 条翻回去了 |
| 6 | 编排器与 SGLang 同机、GPU 机上另写一层 h3-wrapper、视频走 nginx + CDN | 三台机器，编排器独占一台，**没有 wrapper、没有 nginx、没有 CDN、没有 Redis、没有 Vercel** | 5.1 / 5.2 节按实际重写 |
| 7 | 条件帧和成片用 `file://` 路径（隐含 ssh/scp 搬运） | **全程 HTTP**：H3 自己来拉条件帧，成片从 `/content` 取回 | 附录的请求模板照抄会 400 |
| 8 | depth-2 投机展开是"算力富余后的平滑升级"，能把窗口翻倍 | 2 槽下**实测更慢**：玩家感到的停顿从 1.8s 变成 10.8s，已回退。救回窗口的是不占槽的 `PREPARE_AHEAD` | 2.3 杠杆 4/5 换了个方向：该问的不是"要不要更多 GPU"，是"哪些事不需要 GPU、可以提前做" |
| 9 | 降级主旋钮是 `num_inference_steps 50→32` + `quality` 三档 | 步数被蒸馏固定成 **8**，发别的值服务端直接报错；`quality` 也不认。**唯一诚实的旋钮是片长**，且落在 `5 + 17k` 帧格子上 | 第 7 节的降级表按实际重写 |
| 10 | 硬切时用 SD3.5 新画一张关键帧，让图像模型去重建场景 | **画出来的那张图和玩家正在看的画面没有一个像素是共享的**，H3 把它钉在第 0 帧、然后就自由地去听文字了——于是它在片内自己切一刀。实测同一局 21 拍：画了图的 11 拍有 **7 拍在开场 4s 内硬切**（中位 1.7s），接父末帧的 9 拍是 **0/9** | **除开场外不再画图**（`MIDSTORY_KEYFRAMES=0`）。3.2 节整节重写：`transition` 从此描述**故事**而不是剪辑，换地方由镜头运动在片内完成。图像成本从九成降到接近零（第 9 节） |

没改的部分就是仍然成立的部分：核心循环（第 1 节）、世界状态契约（第 4 节）都没被实测推翻；
第 3 节的连续性**策略**成立（末帧衔接是对的），但它的例外条款（硬切重画）被第 10 条推翻了；
第 7 节的**原则**（故事永不中断、fail-open 到模板）也成立，只是它的旋钮换了（见上表第 9 条）。

第 4 节的 TypeScript 接口是**规格**，字段名的权威定义在
`services/orchestrator/app/schema.py`（Pydantic、snake_case），两边有出入时以代码为准。

**还没有实测数字的地方都标了 ⬜**，其中最要紧的三个：末帧链的漂移一次没测过（3.3）——上表第 10 条
之后这条从"周期 K 定得准不准"升级成"**漂移现在完全没人管**"、`PREPARE_AHEAD` 的收益只有 4 拍样本
（2.3 杠杆 5）、内容安全一条没做（第 11 节）。

---

## 0. 一句话产品定义

**不是"AI 生成视频工具"，是"一部你边看边写的电影"。**

衡量成败的唯一指标：**用户做出选择后，下一段影像是否立刻开始播放（零等待）**。
一旦出现 loading spinner，"世界"的幻觉就破了，产品退化成一个视频生成 demo。

因此下面所有架构决策都服务于一件事：**把生成延迟藏进播放时间里**。

---

## 1. 核心循环

```
        ┌─────────────────────────────────────────────────────┐
        │                                                     │
   [世界创建]                                                  │
   用户输入世界观 ──→ 生成 世界圣经 + 三幕大纲 + 主视觉关键帧      │
        │                                                     │
        ▼                                                     │
   [第 0 段] fl2va 生成 14.375s 影像（带音频）                    │
        │                                                     │
        ▼                                                     │
   ┌─ 播放第 N 段 (14.375s) ───────────────────────┐          │
   │          └ 最后 4s：选项浮上来，画面和声音不停 ┐ │          │
   │   ▲ 后台并行：                               ▼ │          │
   │   │  Director 产出 N+1 的两个分支          用户选择        │
   │   │  PromptIR × 2 编译 IR                 A / B /       │
   │   │  H3 × 2 并行生成两段影像                自由输入 ──────┘
   │   │  ffmpeg 抽末帧 + 落到资源目录
   │   └─ 前端 preload 两条视频
   └───────────────────────────────────────────────
```

**关键：两个分支的视频都预先生成好。** 用户选哪个都是瞬时切换，另一条进入故事树缓存（可回溯重玩，不浪费）。

在这之上还有一层 depth-2 的"提前准备"（`PREPARE_AHEAD`，默认开）：孙拍的 IR 编译**在祖父拍播放时就做掉**，但不排 GPU 槽位。这是 2.3 节杠杆 5，也是目前唯一让 beat 墙钟落到窗口以内的手段。（它原先还提前画关键帧，现在没有图可画了——见头表第 10 条。）

---

## 2. 时间预算 —— 本项目最重要的一张表

### 2.1 单拍（beat）串行链路

真实对局的埋点，不是估计。`timings` 逐项落在每个 beat 上，下表是 80 拍的中位数
（`services/orchestrator/app/engine.py` 写入，调试面板的「时间」页直接读它）：

| 阶段 | 实现 | 中位 | 样本 | 能否并行 |
|---|---|---|---|---|
| Director LLM（出 2 个分支 + state delta） | Bedrock **Haiku 4.5**，结构化 JSON | **9.62s**（其中 decode 8.95s，约 1100 out tok） | 80 | 两分支一次调用出，无法再拆 |
| PromptIR 编译 IR | Bedrock Haiku 4.5 + validator + repair | **4.57s**（约 440 out tok） | 80 | 两分支并行；`PREPARE_AHEAD` 下已提前做掉 |
| 关键帧（SD3.5 Large） | Bedrock us-west-2，**现在只有开场那一拍才有**（头表第 10 条） | **6.28s** | 52（全部来自改动之前） | 与 PromptIR 并行 |
| H3 生成 14.375s 影像 | SGLang `/v1/videos` 提交+轮询 | **8.90s**（服务端自报 7.66s，差值是排队） | 80 | 两分支必须打到 2 个实例 |
| 取回成片 | `GET /v1/videos/{id}/content`，内网 HTTP | **6ms** | 80 | — |
| 上传条件帧 | 不存在了：H3 自己来拉（见第 5.1 节） | **0** | 80 | — |
| ffmpeg 抽末帧 + poster + 探时长 | **编排器本地**，不在 GPU 机上 | 0.3–0.6s | 80 | 并行 |
| **GPU 段合计** | | **9.82s** | 80 | |
| **这一拍总墙钟** | 从 Director 起算到成片可播 | **16.48s**（p95 **22.68s**） | 80 | |

三件和 v0.1 相反的事：

- **Director 才是最大的一项**（9.62s > PromptIR 4.57s）。v0.1 把全部优化预算押在 PromptIR 上，
  方向错了。它已经从 8.1s 降到 4.57s（prompt caching + `IR_TEMPLATE_TAIL`，2.3 节杠杆 1 的两条
  都做完了），而 Director 一次要吐两个分支的完整 JSON，约 1100 输出 token，decode 8.95s 就是它的
  全部成本。
- **网络已经不在表里**。`gpu_upload_ms` 恒为 0、`gpu_download_ms` 是 6ms，因为条件帧和成片都走内网
  HTTP（v0.1 假设的 CDN 首字节 0.3s 这一行整个不存在——浏览器那一跳另算，见 5.1）。
- **抽末帧在编排器本地做**，不在 GPU 机上。GPU 机没有宿主机 ffmpeg，而成片反正要落到编排器的资源
  目录，就地抽帧比 `docker exec` 一趟更短也更少一个失败点。

### 2.2 可用窗口

| 组成 | 时长 | 说明 |
|---|---|---|
| 影像段落 | **14.375s** | 345 帧 @ 24fps。帧数格子是 `5 + 17k`，不在格子上的值服务端会静默取整，所以 `gpu.frames_for_seconds` 先对齐再发 |
| 决断时刻 | **+0s** | 选项**压在片尾最后 4s 上**（`DECISION_LEAD_S = 4`），画面和声音不停，不是额外的窗口 |
| 转场 | 0 | 没有 crossfade：`fl2va` 的末帧衔接本来就是像素级连续的，叠一层淡入只会把它糊掉 |
| **合计窗口** | **14.375s** | |

为什么决断时刻不再是 v0.1 那 4s 的定格：这个后端的配乐是**烤进每段影像里**的，没有独立的音乐床，
所以 3.5 节「音乐床不中断」在定格期间根本无法成立——照 v0.1 实现出来的效果是每 18s 里有 4s 是
**无声静止画**。把选项提前 4s 浮上来，picture 和 sound 都不停，切换仍然精确落在片尾边界。

倒计时也撤了（`AUTO_ADVANCE_S = 0`）。它读起来像游戏把选择权抢走，而且什么都没换来：到那个时刻
两个分支都已经生成完，GPU 闲着，催促玩家毫无收益。片尾没选就定格等着，故事等人。
（无人值守演示还留着这条路，只是默认关。）

### 2.3 缺口：p50 2.1s / p95 8.3s，与五个杠杆

**16.48s 的墙钟对 14.375s 的窗口**，缺口不是 v0.1 写的 1.5s——实测玩家真正看到的停顿是
**p50 2.1s、p95 8.3s**（片尾到下一段起播）。p95 才是决定用户会不会看到定格的数，均值没有意义。

1. ✅ **PromptIR 降到 ≤5s —— 做完了，8.1s → 4.57s**

   下面这段分析是对的，结论也兑现了：真正的墙是 decode，砍输出 token 是最划算的一刀。
   落地的两条是 prompt caching 和 `IR_TEMPLATE_TAIL=1`（只生成
   `integrated_multimodal_description`，声景和配乐从世界圣经模板填充——顺带让每拍的音乐描述
   逐字相同，正好是 3.5 节要的东西）。蒸馏到本地小模型那条没做，也不再急：PromptIR 已经不是
   最大的一项了。

   先把 8.1s 拆开，否则会优化错地方：

   | 组成 | 估计 | prompt cache 命中后 |
   |---|---|---|
   | Prefill（guide 摘录 + 36 规则 + 4 gold 示例，粗估 8–15k tok） | 1–2s | **→ ~0.1s** |
   | Decode（IR 三段，粗估 600–1200 out tok @ ~100–150 tok/s） | **5–7s** | 不变 |
   | 网络 + 排队 | 0.3s | 不变 |

   **结论：prompt caching 只省 prefill，8.1s → 约 6–7s。该开（成本 $0.013→$0.005，且每拍间隔 19s ≪ 5min TTL，命中率近 100%），但它不解决延迟。真正的墙是 decode，只跟输出长度和吐字速度有关。**

   按性价比排的 decode 杠杆：
   - **砍输出 token（最划算）**：`overall_soundscape` 与 `non_diegetic_music` 基本由世界圣经的 `musicBible` 冻结，每拍差异极小 → 让 LLM **只生成 `integrated_multimodal_description`**，另两段模板填充。输出量 −40%，decode 降到 3–4s
   - **两个分支并发**调用 Bedrock（8.1s 是单次延迟，不是两次之和）
   - ⬜ **待核实：Bedrock 的 latency-optimized inference 能否用在这条路上。** 我之前写过"Haiku 4.5 支持、通常 +30–60% decode 吞吐"，这个说法我核不实，先撤回：(a) 官方的 Fast Mode 是 Claude API 侧的能力，且只覆盖 Opus 5 / Opus 4.8，不含 Bedrock、不含 Haiku；(b) Bedrock 自己的 latency-optimized 开关是另一回事，但我们推荐走的 Anthropic SDK Mantle client 路径并不暴露它。**结论：把它当成一个待验证项而不是既有杠杆**——如果要用，得先确认该 region + Haiku 4.5 是否在支持列表里，以及是否要为此改用 boto3 Converse（那会牺牲 SDK 侧的 prompt caching 写法一致性）。不要把时间预算押在这条上。
   - 限制 `max_tokens` 并在 prompt 里给出长度上界，不让模型自由发挥
   - Director 输出已高度结构化 → PromptIR 输入更短，少走 repair loop；repair 设 1 次上限
   - **终局方案：把 IR compiler 蒸馏到本地小模型。** 参考实现里的 `oracle.py` 正是为此而生——用 MiniMax 官方 H3-Context-IR API 采 ground truth 做蒸馏（README 明确提到）。7B 模型在 H100 上用 SGLang 跑，decode 可达 1000+ tok/s → **PromptIR 压到 1s 以内**，且 36 条规则的 validator 仍在，质量有硬保底。这条做成后时间预算彻底宽裕，且不再依赖 Bedrock 的网络往返
   - 退路：超时则回退到"模板拼装 IR"（把 Director 的结构化字段直接填进 IR 三段模板，跳过 LLM）。**fail-open 到模板，而不是 fail-closed 到没视频** —— 这是游戏，不是离线批处理

   ⚠️ 参考实现的 8.1s 是 **4 个请求**的均值，样本太小。现在的 4.57s 是 80 拍的中位数，
   `promptir_wall_ms` 与输出 token 数都逐拍落盘。

2. ⬜ **Director 提速 —— 现在这里才是最大的一块（9.62s，decode 8.95s）**

   v0.1 把这条排在第 2 位、只想着"流式让分支 A 先发车"，低估了它。实测 decode 就占 8.95s，
   约 1100 输出 token，一次要写两个分支的完整 JSON + state delta。可选的三条，按性价比：

   - **砍输出**：`consequenceHint`、`shot.mood`、`stateDeltaIfChosen` 里有大量可模板化/可省的字段。
     PromptIR 砍 40% 输出换来近一半延迟，这里同理，且没做过。
   - **流式，A 分支先发车**：先吐完 A 再吐 B，A 的 PromptIR 可提前约 4s 启动。
     `PREPARE_AHEAD` 已经把这 4s 的价值削掉了大半（IR 本来就提前做掉了），所以这条现在
     只对"玩家自由输入"那条冷路径有用。
   - **换模型不是杠杆**：Sonnet 5 在这个账号上 `director_total` 是 27–49s，而且隔离探针显示那是
     平台侧延迟而非 prompt 长度（输入 token 恒定，TTFT 在并发 1 下 13.9–24.5s 抖动）。Haiku 4.5
     已经是快的那一端。

3. ✅ **选项文字与视频解耦 —— 做完了**
   选项 UI 只依赖 Director，**永不阻塞**；卡片上用一个低调的进度指示表示该分支影像的就绪状态
   （`DecisionOverlay` 的 `childStatus`）。视频是"选项的预览与兑现"。

4. ❌ **投机展开（depth-2）—— 试过，实测更慢，已回退到 depth 1**

   v0.1 把它当成"算力富余后的平滑升级"。真做出来之后（eager expansion 让孙拍可规划的那一刻就试了）
   它让游戏更慢：

   |  | depth 2 | depth 1 |
   |---|---|---|
   | `gpu_total`（真实生成） | 中位 13.5s | 中位 13.0s |
   | 等槽位 | 中位 **28.5s** | 中位 11.5s |
   | 玩家实际感到的停顿 | 中位 **10.8s** | 中位 1.8s |

   算术就是全部原因：depth 2 要 2 个子 + 4 个孙 = 6 条片子抢 2 个槽，而一条片子是 13s 的 GPU、
   一旦开跑不可抢占。`PRIORITY_SPECULATIVE` 只决定谁拿**下一个**空槽，所以玩家马上要用的那两个子拍
   还是排在投机后面三层。**规则：`slots >= branch_count ** depth`。** 2 槽 × 分支 2，depth 1 恰好
   打满，depth 2 是 3 倍超售。等实例多了再调 `PREGEN_DEPTH`，在那之前别动。

5. ✅ **提前准备（`PREPARE_AHEAD`，默认开）—— 目前唯一让墙钟落进窗口的手段**

   和 depth-2 看起来像，实际是另一种资源：depth-2 超售的是**不可共享**的 GPU 槽，这条**一个槽都不占**。
   它只把"卡住一拍开头的那次 Bedrock 调用"搬到祖父拍播放的时候做掉——孙拍的 IR 编译。依据和
   `_ensure_frontier` 的 Director 前瞻是同一条：这件事只依赖 beat 的 `intent` 和 `state_after`，
   `_make_child` 一跑就都有了，跟它的视频毫无关系。

   | | 关键路径 | 对 14.375s 窗口 |
   |---|---|---|
   | 一拍（改动前，要画图） | max(PromptIR 4.6, 关键帧 6.3) + GPU 9.9 = **16.2s** | 超了 |
   | 一拍（现在，接末帧） | PromptIR 4.6 + GPU 9.9 = **14.4s** | 正好卡死 |
   | 提前准备过的一拍 | 只剩 GPU 9.9s | **富余 4.5s** |

   实测提前准备过的 beat 墙钟中位 **12.24s**（样本只有 4 拍，还要补）。
   代价是每移动一次光标要准备 4 个孙拍、至多一半会被播到——但那 4 次调用现在只是 PromptIR
   （Haiku，约 440 out token，可忽略）。**它原本真正的代价是翻倍的 SD3.5 图钱，而那笔钱已经
   随头表第 10 条一起没有了**，所以这个旋钮不再是"钱换延迟"，是白拿的 4.5s。

### 2.4 首拍（冷启动）：不是 21s，是约 110s

开场没有"上一段在播"来掩盖延迟。实测（5 局）：

| 阶段 | 实测 | 说明 |
|---|---|---|
| Worldsmith 写世界圣经 | **89.7s**，首 token 就等 **52.4s** | Sonnet 5，一次调用写完圣经 + 三幕大纲；`worldsmith_ttft_ms` / `worldsmith_out_tokens` 逐局落盘 |
| 开场关键帧 | 6.3s | SD3.5 |
| PromptIR | 4.6s | |
| H3 | 9.9s | |
| **合计** | **≈ 110s** | |

**这是目前最大的单项短板，而且不在原设计的视野里**（v0.1 估 4s，实际 89.7s，差 20 倍）。
52.4s 的 TTFT 说明大头是平台侧延迟 + 一次性吐完一份很长的 JSON，不是 prompt 太长。

两条待做的路，都还没做：
- **拆成两次调用**：先出"玩家立刻要读的东西"（logline / 你是谁 / 世界的规则 / 开场关键帧提示词），
  立刻起开场仪式并开始画关键帧；三幕大纲和次要角色第二次调用补齐——它们要到第 2 拍才被用到。
  这能把首屏从 90s 压到 20–30s。
- **开场仪式做够 110s 的量**。现在的 `curtain` 只有关键帧 + logline + 一行诊断文案，撐不住 110s。
  v0.1 列的那几条仍然是对的方向，只是量要按 110s 而不是 21s 设计：
  关键帧渐显 → 世界圣经逐行打字机（地点 / 你是谁 / 世界的规则 / 你要什么）→ 环境音床先起 →
  "世界正在成形 …"的诊断式进度。

用户在读设定时，等待可以是仪式；但 110s 的仪式得真的有 110s 的内容，否则就是 spinner。

---

## 3. 视觉与听觉连续性

这是"开放世界"和"一堆不相干的 AI 视频"之间的分界线。

### 3.1 帧级衔接：末帧 → 首帧（i2va）

所有段落（除开场）统一走 `task: "fl2va"`，只给首帧：

```jsonc
{
  "task": "fl2va",
  "conditions": [
    // 不是 file://。给的是一个 http URL，H3 自己来拉——见 5.1。
    { "type": "image", "uri": "http://172.31.42.3:8101/<sid>/kf-b3-c1f36e.png",
      "role": "keyframe", "frame_index": 0 }
  ]
}
```

成片取回之后，**编排器本地**（不是 GPU 机上）`ffmpeg -sseof -0.1 -i beat.mp4 -frames:v 1 last.png`，
末帧图同时用于：① 下一段的首帧条件 ② 前端片尾定格的底图（与视频末帧像素级一致，零跳变）。
**一次抽帧，两个用途**，而且第二个用途也是第一个用途的验证工具：
`bench/frame_continuity_check.py` 把每段 mp4 的第 0 帧和它"应该起自"的那张图做归一化互相关，
连续拍对父末帧、硬切拍对自己的关键帧，实测 13 拍全部 0.999–1.000。
这条链是这个项目里唯一可以逐帧验证对错的部分，值得有一个专门的量具。

（副作用：一个"下一段开头画面不对"的报告，可以先用这个量具判断是管线给错了图、还是前端在双缓冲
交换时画了上一个住客的残帧。实测是后者，修在 `web/components/VideoStage.tsx`。）

**但这个量具有一条它天生看不见的失效模式，而且那条恰好更常见。** `fl2va` 只**钉住第 0 帧**，第 30
帧它一个字都没承诺——所以"第 0 帧永远 0.999"和"片子在 1.4s 处自己切了一刀"可以同时成立，玩家看到
的是后者。这就是头表第 10 条的发现过程，量它要另一个判据：
`bench/internal_cut_scan.py` 看**逐帧相邻**的相关性序列，硬切是一个**台阶**（某一对相邻帧掉到
0.8 以下，而它两边都还在 0.99），而不是一个低分。
拿"第 0 帧 vs 第 240 帧的低分"当硬切判据会在 21 拍里报 20 拍——14 秒的运动镜头本来就会掉下任何
固定阈值。**这个错误犯过一次**，记在这里是因为它比它揭示的 bug 更容易再犯。

### 3.2 换场景怎么办 —— 不是切，是把镜头运过去

v0.1 的答案是"硬切时用 SD3.5 新画一张关键帧"，**这个答案是错的**，而且错得比成本更难看。

Director 仍然输出 `transition: "continuous" | "cut" | "timeskip"`，但它现在描述的是**故事**（这一拍
和上一拍是同一个连续动作，还是换了地方 / 过了时间），**不是剪辑方式**。三种值现在都从父拍的末帧起手：

- `continuous` → 承接上一拍的结束姿态，同一个镜头继续
- `cut` / `timeskip` → 还是从末帧起手，但 IR 要求 H3 用**镜头运动或人物移动**在这 14s 之内把观众
  带到新地方：跟着人物走过去、镜头摇过去推过去、穿过门和走廊。所以 Director 的约束也跟着改了
  （`prompts.py` 硬约束 5）：换的地方必须是 14s 内到得了的，「她推门走进后院」可以，
  「她已经站在山顶上」不行。

**为什么不画图了。** 一张新画的图和玩家此刻正在看的画面**没有一个像素是共享的**。`fl2va` 把它钉在
第 0 帧，之后模型就自由了——它拿着一张跟文字不符的陌生画面，最省力的解法就是演一小会儿、然后自己
切一刀切到文字描述的那个场景去。实测同一局 21 拍：

| | 拍数 | 开场 4s 内自己切了一刀 | 最早 |
|---|---|---|---|
| 画了新关键帧 | 11 | **7**（中位 1.7s） | 第 1 帧 |
| 接父末帧 | 9 | **1**（3.2s） | — |
| 开场（英文提示词直出） | 1 | 0（全片最小逐帧跳变 0.998） | — |

还有一层原因让画出来的那张图格外不靠谱：`keyframe.py` 把 Director 写的**中文** `setting` / `mood`
插进一串英文 SD3.5 提示词里。同一个 seed 三个版本比一比：把中文删掉，图几乎不变（A~C 0.800）；
把中文**翻译**成英文，图大变（A~B 0.386）。也就是说那几段中文一直是**无效输入**，SD3.5 只好从人物
描述里自己编一个环境出来——它把「门厅到一楼楼梯间，昏黄的壁灯」画成了大白天的室外石板广场。
H3 读中文没问题，SD3.5 不读，这个**不对称**才是那张图走样的直接原因。

这条路现在只在一个地方还留着：父拍的末帧文件不存在（父拍失败）。那时候画一张也比没有好。
要把整套旧行为拿回来：`MIDSTORY_KEYFRAMES=1`（默认 0）。**真要重新启用，先把 `shot.setting_en`
加到 Director 的输出里**，别再喂混合语言的提示词。

**Director 输出的硬切率仍然是 65% 左右**（81 拍里 cut 48 + timeskip 5，`SCENE_MAX_BEATS=3` 决定的
——一拍 14.4s，三拍 43s 已经是一个场景的长度上限）。区别只是这 65% 现在不再花钱、也不再产生
陌生画面：它变成了对 IR 的一句"请在片内运镜过去"。

### 3.3 抗漂移：定期重锚定

末帧链式传递会累积色偏、锐度衰减、角色特征漂移。现在的对策只剩第一条：

- ✅ 世界圣经里冻结 **style anchor**（胶片规格、镜头、光比、调色）和 **character sheet**（每个角色的
  外观描述，逐字复用），每次 IR 都注入
- ✅ 记录每段末帧的亮度/饱和度/锐度相对本链锚点的偏离（`Beat.drift`，调试面板可见）、超阈值置
  `needs_reanchor`
- ⬜ **但 `needs_reanchor` 现在没有接任何东西。** 它原先会强制插一次 `cut` 并用 SD3.5 重画，而那正是
  3.2 节量出来会造成片内硬切的动作——**用一个每次都看得见的 bug 去换一个还没测到过的漂移，不划算**，
  所以这条通路连同 `REANCHOR_EVERY` 一起在默认下关掉了（跟着 `MIDSTORY_KEYFRAMES` 走）。

⬜ **于是漂移现在是完全无人管的**，而且 20 拍的链一次都没量过。`bench/chain_drift.py` 就是为这个写
的，它现在的优先级比之前高：要么它证明 20 拍下漂移可以忽略（那这一节可以整节删掉），要么它给出一个
真实的 K，那时候重锚定要用**别的**手段实现——比如让 IR 显式把 style anchor 和角色外观重新写满一遍，
而不是再去画一张没人认得的图。

### 3.4 `ref2va` 的取舍（重要架构约束）

`--model-variant` 是**启动参数**，一个 SGLang 实例只能是 `fl2va` 或 `ref2va` 之一。
- **两个实例都起 `fl2va`**（MVP 决策）：拿到帧级连续性，这是游戏性的刚需
- `ref2va` 的角色参考图能力很诱人（角色一致性更强），但会牺牲帧衔接。留作 v2：起第 3 个实例专门做"角色特写/新角色登场"这类镜头

### 3.5 音频连续性

H3 每段自带音频，段落边界会有音乐断点。三层处理，只做到了第一层：

1. ✅ 世界圣经冻结 `music_bible`（BPM、调性、编制、情绪基线），每段 IR 的 `non_diegetic_music` 字段
   复用同一描述 → 风格连贯。`IR_TEMPLATE_TAIL=1` 让它变成**逐字相同**，比原计划更强
2. ⬜ **前端叠一条持续不断的低音量音床** —— 没做，而且这条得重新想。配乐是**烤进每段影像里**的，
   不是独立轨，所以第二条音床会和片内配乐打架而不是掩盖接缝。这个约束还连带否掉了 v0.1 的
   4s 静止定格（见 2.2）
3. ⬜ 段落交界 200ms 音频 crossfade —— 没做。做了就要连画面一起淡，会把 `fl2va` 的像素级衔接糊掉，
   所以这两件事得一起决定

⬜ 接缝到底有多明显还没量化（第 8 节第 7 项），所以上面两条该不该做也还没有依据。

---

## 4. 世界状态：从"漂流"到"故事"

纯 prompt 链会迅速退化成语义随机游走。必须有结构化状态 + 长期目标。

### 4.1 世界圣经（World Bible，创建时生成一次，全局不变）

```ts
interface WorldBible {
  id: string;
  premise: string;            // 用户原始输入
  genre: string;              // 末日 / 现代都市 / 修仙 / ...
  logline: string;            // 一句话故事
  styleAnchor: string;        // 冻结的视觉语法，逐字注入每个 IR
  musicBible: string;         // 冻结的音乐语法
  protagonist: Character;     // 玩家扮演谁（第一人称 POV 还是第三人称跟随）
  pov: 'first' | 'third';
  characters: Character[];    // 角色外观圣经，防漂移
  worldRules: string[];       // 该世界的硬规则（修仙：境界体系；末日：感染机制）
  outline: ActOutline[];      // 三幕大纲：给 Director 的长期指北
  openingKeyframeUrl: string;
}

interface Character {
  id: string; name: string;
  appearance: string;   // 逐字复用，不允许 Director 改写
  voice: string;        // 音色描述，给 IR 的对白用
  arc: string;
}

interface ActOutline {
  act: 1 | 2 | 3;
  milestone: string;    // 该幕必须达成的叙事里程碑
  targetBeats: number;  // 预算拍数
}
```

### 4.2 可变状态（每拍更新）

```ts
interface WorldState {
  beatIndex: number;
  currentAct: 1 | 2 | 3;
  location: string;
  timeOfDay: string;
  elapsedInWorld: string;     // 世界内流逝时间
  presentCharacters: string[];
  inventory: string[];
  stats: Record<string, number>;  // 由世界观决定：修仙=灵力/境界，末日=体力/物资/感染度
  flags: Record<string, boolean | string>;  // 后果系统：谁死了、谁记恨你、哪扇门开了
  tension: number;            // 0-1，Director 用来控节奏
  summary: string;            // 压缩的前史，每 8 拍重写一次，防止无限膨胀
  recentBeats: string[];      // 最近 3 拍的原文摘要
}
```

**后果系统是"有重量的选择"的全部来源**：Director 的 system prompt 强制要求——每个新分支必须引用至少一个已有 `flag` 或 `stat`，否则选择就是装饰。

### 4.3 Director 输出契约

一次调用同时产出两个分支（省一次往返），严格 JSON schema + 校验：

```ts
interface DirectorOutput {
  narration: string;            // 给 UI 的一句旁白/章节标题
  stateDelta: Partial<WorldState>;
  options: [BranchIntent, BranchIntent];
  predictedChoice: 0 | 1;       // 槽位优先级与提前准备的排序依据（depth-2 已回退，见 2.3）
  isEnding?: { type: string; title: string };
}

interface BranchIntent {
  label: string;                // 选项文案，≤14 字，动词开头
  consequenceHint: string;      // 给玩家的模糊代价暗示（不剧透）
  transition: 'continuous' | 'cut' | 'timeskip';
  shot: {
    type: 'wide' | 'medium' | 'closeup' | 'pov' | 'tracking' | 'aerial';
    subject: string;
    action: string;             // 14.4 秒内能演完的**一个**动作
    setting: string;
    mood: string;
    dialogue?: { speaker: string; line: string }[];  // 交给 IR 的对白，注意音节预算
    sfxFocus: string;
  };
  stateDeltaIfChosen: Partial<WorldState>;
}
```

**镜头多样性约束**：Director 的 prompt 里注入最近 3 拍的 `shot.type`，禁止连续三拍同机位。否则 14.4s × N 段会视觉催眠。

**动作单一性约束**：14.4 秒只能演一个动作。Director 最常犯的错是塞进"他推开门、穿过走廊、爬上屋顶、看见飞船"——H3 会做出一团糊。system prompt 里要有反例。

### 4.4 大纲牵引

Director 每拍都收到：`当前幕 / 该幕里程碑 / 已用拍数 / 预算拍数`。
- 拍数过半仍未接近里程碑 → 提升 `tension`，分支必须推进主线
- 里程碑达成 → 进入下一幕，`summary` 重写
- 第三幕里程碑达成 → 输出 `isEnding`，进入结局段（多结局，由 flags 决定哪一个）

这是"无限漂流"和"一部有结构的电影"的区别。

---

## 5. 系统架构

### 5.1 部署拓扑（实际跑着的）

三台 EC2，us-east-2，同 VPC 同安全组。没有 wrapper、没有 nginx、没有 CDN、没有 Redis、没有 Vercel、
没有 S3——v0.1 那张图里的中间层一层都没有实现，而且没有一层是缺的。

```
┌───────────────────────────────┐
│ 浏览器 + next dev :3000（本机） │
└───────────┬───────────────────┘
            │ ssh -L 127.0.0.1:8100 → 控制接口与 /assets（唯一出 region 的一跳）
            ▼
┌────────────────────────────────────────────────────────────┐
│ 编排器  172.31.42.3 / ec2-13-59-29-243                      │
│   uvicorn app.main:app  →  127.0.0.1:8100                  │  会话状态机 / 拍流水线
│                              会话 · 选择 · SSE · /assets     │  槽位调度 / 故事树
│   只读静态文件服务       →  0.0.0.0:8101                     │  ffmpeg 抽末帧+poster
│                              只有 ASSETS_DIR，只答 GET/HEAD   │  DATA_DIR 落盘存档
└────┬──────────────────────────────────────────┬────────────┘
     │ POST /v1/videos                          │ POST /v1/videos
     │ GET  /v1/videos/{id}/content             │
     ▼                                          ▼
 P5-1 172.31.45.68:30010                  P5-2 172.31.33.181:30010
 SGLang Diffusion · fl2va · 8 GPU         SGLang Diffusion · fl2va · 8 GPU
     │                                          │
     └──── GET 172.31.42.3:8101/<sid>/kf.png ───┘   ← 条件帧是 H3 自己来拉的
                          │
                          ▼
     Bedrock us-east-1（Worldsmith / Director / PromptIR）
     Bedrock us-west-2（SD3.5 Large 关键帧）
```

**两个 socket，两种暴露面。** 这是这一节唯一重要的设计决定：条件帧必须让 GPU 机拉得到，而控制接口
自己没有任何鉴权——会话创建、选择、以及背后的 GPU 预算都在它后面。所以拆成两个：

| | 绑定 | 提供什么 | 谁能进 |
|---|---|---|---|
| 控制接口 | `127.0.0.1:8100` | 全部 API + `/assets` | 只有 ssh 隧道 |
| 资源服务 | `0.0.0.0:8101` | `ASSETS_DIR` 里的图和片，`StaticFiles` 只答 GET/HEAD | 同安全组的 GPU 机 |

暴露面因此是**对象本身的属性**，不是某个中间件的行为：`_assets_app()` 这个 Starlette 应用里
根本没有 POST 可以忘记保护。实测从 P5-1：关键帧 GET 200（1,845,222 B）、POST 405、`:8100/healthz`
不可达；本地再用 ASGITransport 验了三种路径穿越都是 404。

**安全组从来不是障碍。** 直连 `:30010` 回的是 `Connection refused`（内核 RST），而被安全组拒绝的包
是**静默丢弃**的——这两个现象不一样。真正的原因只是 SGLang 绑在 127.0.0.1 上。改成 `0.0.0.0`
之后 ssh 隧道（`deploy/h3-tunnel@.service`）就退役了，文件留着并注明怎么恢复。

**唯一出 region 的那一跳，也是最慢最抖的一跳。** 编排器在 VPC 里取成片 6ms；浏览器取同一个文件要走
笔记本的 ssh 隧道，而笔记本的默认路由还是一条 VPN（`utun4`，MTU 1390）。同一个 1.7MB 片子，同一分钟内
实测先 **15.9s（107KB/s）**、再 **2.5s（670KB/s）**，连 `/healthz` 都量到过 2.3s。两个后果都处理了：

* **隧道交给 launchd 守护**（`deploy/local_proxy.sh`）：`kill -9` 之后 9s 自己回来。在这之前它死了就
  一直死着，而表现是"后端一切正常、页面还开着、就是什么都放不出来"——因为页面早就加载完了，
  死掉的只有取视频那一跳。
* **成片在 `_postprocess` 里统一 remux 成 faststart**：H3 出的 mp4 是 `ftyp / free / mdat / moov`，
  索引在 1.7MB 的**末尾**，浏览器必须先为尾部发一个 range 请求才能解出第一帧。快链路上这只是多一次
  往返，这条链路上它是多一次卡住的机会。`-c copy`、65ms、345 帧与时长一字不差
  （`bench/faststart_check.py` 验的），存量 107 条也补过了一遍。

**为什么编排器独占一台、而不是 serverless：** 每拍的流水线是跨请求的有状态长流程，serverless 里要
拆成 queue + 外部状态存储，多好几跳网络；而且它和两台 GPU 机在同一个 VPC，取成片 6ms。
状态直接落在 `DATA_DIR` 的文件里（`session.json` + `events.jsonl`），没有 Redis：单进程单写者，
文件就是够用的，而且事后可审计、可重放。

### 5.2 没有 GPU wrapper（编排器直连 SGLang）

v0.1 要求"必须自己写一层"。写了骨架（`services/h3-wrapper`）但没有用上，因为下面这 6 步里没有一步
非得在 GPU 机上做：

| v0.1 要 wrapper 做的 | 实际在哪做 |
|---|---|
| 组装 `/v1/videos` 请求 | 编排器（`gpu.H3Backend._body`） |
| 调 SGLang、落盘 mp4 | 编排器提交 + 轮询，成片从 `/content` 取回 |
| ffmpeg 抽末帧 / 探时长 / poster | **编排器本地**：GPU 机的宿主机没有 ffmpeg，而成片反正要落到编排器 |
| 末帧直方图（漂移检测） | 编排器（`Beat.drift`） |
| 移到 `/var/www/clips/` | 落到 `ASSETS_DIR`，由 8101 直出 |
| 异步上传 S3 | 没做，也不需要：`ASSETS_DIR` 在 EBS 上，重开存档还在 |

`timings` 那条要求仍然成立，而且做到了：`gpu_upload_ms` / `gpu_server_ms` / `gpu_sglang_ms` /
`gpu_download_ms` / `gpu_onbox_ms` / `gpu_postprocess_ms` 逐项落在每个 beat 上（调试面板「时间」页）。
**没有细粒度埋点就没法调优**——2.1 节整张表和 depth-2 的回退结论都是这些数字给的。

`WrapperBackend` 留在代码里（`GPU_BACKEND=wrapper`），是给"GPU 机不能回连我们"那种拓扑留的后路，
和 `H3_TRANSPORT=ssh` 同一个用途。

### 5.3 GPU 槽位调度器

2 个 SGLang 实例 = **2 个并发生成槽**。

| 模式 | 每会话占用 | 支持并发会话 | 代价 |
|---|---|---|---|
| **双分支预生成**（现在） | 2 槽 | **1** | 片尾 p50 2.1s / p95 8.3s |
| 单分支预生成（按 `predicted_choice`） | 1 槽 | 2 | 约 40% 概率触发 ~14s 等待 |
| depth-2 投机 | 6 槽 | 0（需扩容） | 见 2.3 杠杆 4：**2 槽下实测更慢** |

**规则：`slots >= branch_count ** depth`。** depth-2 那一行 v0.1 写的是 3 槽，错了——它要的是
2 个子 + 4 个孙 = 6 条并发片子。2 槽下是 3 倍超售，实测把玩家感到的停顿从 1.8s 推到 10.8s。

调度器实现：优先级队列，`priority = (是否阻塞播放) × (会话活跃度) × (predicted_choice 权重)`。
用户已做出的选择所在分支永远最高优先级——它可能还没生成完，而这正是 p95 8.3s 那条尾巴的来源。

⬜ 候场室（排队位次 + 先写世界观设定）还没做。现在是单会话，第二个人进来就抢槽。

---

## 6. 前端：无缝播放的实现细节

### 6.1 三个 video 元素的池子（`VideoStage`）

三个，不是两个：两条分支都预生成，决断时刻前**两条都得 buffer 好**，所以是
当前 + 分支A + 分支B。常驻、永不 unmount——unmount 会拆掉再重建硬件解码器，表现为一两帧黑闪，
正好在我们用整条 `fl2va` 链花大力气抹平的那个接缝上。所以 `src` 用 ref 命令式地赋，交换只是
已经在解码的元素之间改 `opacity`。

池子按 **beat id** 寻址（"buffer 这一拍" / "show 这一拍"），槽位记账（含淘汰）全部关在组件里。

**"永不 unmount"的另一半代价，是元素会把上一段的画面带进下一段。** 换 `src` 并不清空合成器里
已有的那一帧：元素会继续画它上次解出的最后一帧，直到新片子解出第一帧。所以"设完 src 立刻揭幕"
会把这个槽位的**上一个住客**（没被选的那条分支，或者更早的一拍）画出几帧——这就是玩家报的
「有的时候前几帧是其他画面」。它只在槽位是冷的时候发作：玩家选的那一支在光标已经移过去之后才
生成完（p50 2.1s，很常见），或者从故事树跳到一个没在池子里的拍。

修法：`show()` 要等这个元素真的持有**本片**的一帧（`loadeddata`）才揭幕，上限 4s。等待期间屏幕上
是上一段的末帧——对连续拍来说那正是下一段的条件帧，像素级一致，所以等待是看不见的；超时才硬揭
（宁可一帧残影，不要黑屏，见第 7 节原则）。两个随之而来的不变量：等待期间那个槽位不可被淘汰，
以及 `currentTime = 0` 必须在揭幕之前做完。

（怎么判断报告到底是"管线给错图"还是"前端画了残帧"：见 3.1 的 `frame_continuity_check.py`。）

### 6.2 决断时刻：压在片尾最后 4s 上，而且没有倒计时

v0.1 写的是 `ended` → 定格 → 选项飞入 → 4s 倒计时。照这个实现出来的效果是每 18s 里有 4s 是
**无声静止画**，因为这个后端的配乐烤在每段影像里，没有独立音乐床（见 2.2）。现在是：

- `onTimeUpdate` 在**距片尾 4s**（`DECISION_LEAD_S = 4`）把选项浮上来，**画面和声音都不停**
- 中途点了 → 记下来，不立刻提交（提交会移动服务端光标、把玩家还在看的这段切掉），
  到 `ended` 那一刻才 commit，所以交换精确落在片尾边界
- `ended` 之后才铺 `last_frame_url` 的 `<img>`（与视频末帧像素一致），做极缓慢的 Ken Burns 推镜。
  它不是为了防黑屏（暂停的 `<video>` 本来就停在末帧上），是为了让"无限期的定格"读起来像一个
  **被刻意拉长的定格镜头**而不是卡住
- 这张图要**预加载并且等它 decode 完才揭**：`last.png` 是 290–740KB 的无损 480p 帧（因为同一个
  文件还要当下一拍的条件帧，有损压缩会沿链累积），走 ssh 隧道实测 0.7–1.3s。服务搬到 us-east-2
  之后，一个没加载完的 `<img>` 盖在视频上 1 秒，就是"视频播完后会闪一下"的全部原因
- **没有倒计时**（`AUTO_ADVANCE_S = 0`）。片尾没选就定格等着，故事等人。理由见 2.2：到那一刻两个
  分支都生成完了，催促玩家换不来任何东西，只换来"游戏替我做了决定"
- 片尾还提供「再看一遍」（右下角）：这一刻它是免费的——片子已经在槽位里解好，也没有倒计时在跑

### 6.3 第三条路：自由输入 ✅

选项区常驻一个"或者…"输入框。用户输入任意行动 → 无法预生成 → 末帧定格 + "世界在回应你…" +
回显他自己写的那句话。实测这条路要等 Director 9.6s + PromptIR 4.6s + H3 9.9s ≈ **24s**，
比 v0.1 估的 8–20s 更长。（改动前这一段是 max(PromptIR 4.6, 关键帧 6.3)，所以头表第 10 条在这里
只省下 1.7s——关键帧本来就和 PromptIR 并行，它值钱不值时间。）

和选项不同，自由输入是**立刻提交**的：这条路后面什么都没有预生成，所以还在播的每一秒都是从可见
等待里省下来的一秒。

**这个等待是可接受的**，因为用户主动偏离了预设路径，心理上预期"我要求了特别的东西"。这恰恰是"开放世界"感的来源，不要因为延迟而砍掉。

### 6.4 故事树面板 ✅

DAG 可视化。未选择的分支视频已经生成好且缓存 → 点击即可回到岔路口重走，**几乎零边际成本**。这是双分支预生成的免费红利，也是留存钩子。

另外做了历史记录（主页列出所有存档、可删除）和调试面板（按 `D`：时间 / 这一拍的 IR 与关键帧提示词 /
世界圣经 / SSE 事件）。调试面板不是开发工具而是产品的一部分证据链：2.1 节那张表就是它读的同一份
`timings`。

### 6.5 导出与分享 ⬜ 没做

- `POST /export` → ffmpeg concat 用户路径上所有 clip + 交界 crossfade + 片头片尾字卡 → 一部 2–5 分钟的"你的电影"，可下载/分享
- 分享链接编码选择路径 → 别人可以看你的版本，并**从任意一拍分叉出自己的故事**

素材是齐的（每拍的 mp4 都在 `ASSETS_DIR`，路径在 `session.json` 里），差的只是这一个端点。

---

## 7. 降级与兜底（必须有，GPU 会抖）

| 故障 | 降级策略 | 状态 |
|---|---|---|
| 分支视频未就绪且用户已选 | 末帧定格 + "世界在回应你…"，绝不黑屏/spinner（音乐床这一条做不到：配乐烤在片子里） | ✅ 这是 p50 2.1s 那条路，天天在走 |
| H3 超时 | 重试一次并降参：**只降时长** `14.375s → 10.125s`（243 帧，下一格） | ✅ |
| H3 二次失败 | 拿一张静帧做 Ken Burns 幻灯 + 旁白，剧情继续（"静帧章节"） | ⬜ 没做。注意底图要换成**父拍的末帧**：mid-story 已经没有关键帧可用了（3.2） |
| PromptIR validator 失败/超时 | 回退模板拼装 IR（Director 字段直填三段模板），**fail-open** | ✅ `ir_source=template`，调试面板看得到 |
| Director JSON 不合 schema | 重试一次 → 仍失败则"通用二选一"兜底 | ✅ 就是玩家报过的"只有继续向前/退回原路"——那次是 schema 一直不过，兜底生效了，但兜底本身太朴素 |
| 视觉漂移超阈值 | 原方案：强制 `cut` + SD3.5 重锚定关键帧 | 🟡 只剩检测：`needs_reanchor` 照样置位并发事件，但默认不再触发重画（3.3 说明了为什么） |
| 单个 SGLang 实例掉线 | 冷却 `GPU_COOLDOWN_S`，槽位降到 1，按 `predicted_choice` 生成 | ✅ |
| 关键帧模型不可用 | 合成一张渐变占位图（`KEYFRAME_FALLBACK`），故事照样开场 | ✅ 现在只关系到开场那一拍，但那一拍是最脆弱的一拍（玩家还什么都没看到） |
| 父拍的末帧文件不存在 | 退回去画一张关键帧（3.2 里唯一还会走到的那条） | ✅ 会打 warning，因为它同时意味着父拍出了问题 |
| 后端一切正常、浏览器就是放不出来 | 片子统一 remux 成 faststart（少一趟尾部 range 请求）+ 隧道由 launchd 守护自愈 | ✅ 两条都上了，见 5.1 最后一段 |

**`steps` 不再是降级旋钮。** v0.1 把 `steps 50→32` 当主要旋钮，这条在这个部署上不成立：
checkpoint 是 Stage-DMD 蒸馏版，只烤了 9 个 sigma 格点（8 次 DiT forward），服务端对其他值直接报
`got num_inference_steps=8. Use MiniMaxAI/MiniMax-H3 for other schedules.`。所以步数是**权重的属性**
而不是我们能拧的旋钮，`NUM_INFERENCE_STEPS=0` 表示"别发这个字段"。同理 `quality` 也不发。
**这个部署上唯一诚实的延迟旋钮是片长**，而且它落在 `5 + 17k` 的帧数格子上。

原则：**故事永不中断**。任何一层失败，都要有一个仍然能推进叙事的形态。

---

## 8. 上线前必测清单 —— 哪些已经有答案了

| # | 要测的 | 答案 |
|---|---|---|
| 1 | `/v1/videos` 同步还是异步？返回什么？ | ✅ **异步**：POST 回 `{id}`，轮询到 `succeeded`，成片用 `GET /v1/videos/{id}/content` 取（真 mp4 字节，有 `ftyp`；响应没有 `Content-Type` 头）。轮询响应里的 `url` 一直是 null——GAME.md"没有下载端点"那条结论在这个构建上是**错的**，大概就是被这个 null 误导的 |
| 2 | `fl2va` 实例能否跑纯 t2va？ | ✅ 不能，`conditions` 必须给。所以开场必须靠外部图像模型出关键帧，当前设计正是这么做的 |
| 3 | i2va 相对 t2va 慢多少？ | — 问题作废（t2va 跑不了），直接量到 fl2va 本身：服务端 7.66s，端到端 9.82s |
| 4 | `num_inference_steps` / `quality` 的质量-延迟曲线 | ✅ **没有曲线**：步数被蒸馏固定成 8，其他值直接报错（见第 7 节）。唯一的旋钮是片长 |
| 5 | 同实例并发 2 请求 vs 2 实例各 1 | ⬜ 没单独测。但 depth-2 的实测（2.3 杠杆 4）已经从另一头给出结论：**2 实例就是 2 槽，不多不少** |
| 6 | 末帧链续接 6 段后的漂移 | ⬜ 没测。`bench/chain_drift.py` 就是为这个写的，而 3.2 节之后链更长了（几乎每拍都在链上）、重锚定又关掉了，所以这条从"K 定得准不准"变成了**这版唯一还没兜住的连续性风险** |
| 7 | 段落边界的音频接缝 | ⬜ 没量化。而且 v0.1 给的解法（垫一条持续音乐床）在这个后端上行不通：配乐烤在每段里，垫第二条只会打架 |
| 8 | PromptIR 的 prefill / decode 分项 p50/p95 | ✅ 80 拍：`promptir_wall` 中位 4.57s，约 440 输出 token。prefill/decode 没拆开埋（Bedrock 不回分项），但"砍输出"的收益已经用 `IR_TEMPLATE_TAIL` 兑现了 |
| 9 | 时长上限是否稳定 | ✅ 上限不是 15s：帧数格子是 `5 + 17k`，345 帧 = **14.375s** 稳定；不在格子上的值服务端静默取整，所以客户端先对齐 |
| 10 | `short_edge` 480 vs 768 的延迟差 | ⬜ 没测。但服务端 `--warmup-resolutions` 无论如何会热 768p，所以 `H3_WARM` 存在的意义是：不预热的话，玩家等的**第一条**片子要额外付约 0.9s 的 shape change（GAME.md 发现 1）。现在线上 `H3_WARM=0`，因为 SGLang 是冷启的，这 0.9s 还在 |

另外两条清单上没有、但实测很要紧的：

11. **multipart 那条路是死路。** `input_reference` 不满足 `fl2va`，`seconds` 是 int 类型，
    `num_frames` 不支持。要给条件帧只能走 `conditions[].uri`。记录在 `bench/probe_http_only.py`。
12. **`conditions[].uri` 收 http URL。** 实测服务端从我们这台机器拉走了 2,044,883 字节。
    这一条把整套 ssh/scp 搬运替换掉了（见 5.1），也是 v0.2 第 7 项。

---

## 9. 成本量级（每**播出**一拍）

按"玩家真的看了一拍"折算，把预生成的浪费算进去——这才是账单的口径：

| 项 | 单价 | 播出一拍实际发生几次 | 成本 |
|---|---|---|---|
| Director（**Haiku 4.5**，约 1.1k out） | ~$0.008 | 1 | ~$0.008 |
| PromptIR（Haiku 4.5，约 440 out，system 缓存） | ~$0.0025 | 2 个子 + 至多 4 个孙（`PREPARE_AHEAD`）≈ 6 | ~$0.015 |
| **SD3.5 Large 关键帧** | ~$0.08 | **每局 1 张**（开场），折到 20 拍 ≈ 0.05 | **~$0.004** |
| H3 生成 | 自建 GPU | 2 条 | 约 20 GPU·s |
| 存储 | EBS | 约 3MB/拍（实测 216MB / 73 拍） | 可忽略 |
| **外部账单合计** | | | **~$0.027 / 拍** |

一局 20 拍 ≈ **$0.55**，其中 **85% 是文本**。而文本里最大的一项不是 Director 而是 **PromptIR**
（$0.015 vs $0.008）：单次它只有 Director 的三分之一，但一拍要为 6 个候选拍编译 IR。

这张表刚被改写过一次，值得把两版并排留着，因为它是"一个画面 bug 的修法顺带解决了成本结构"的完整例子：

| | 关键帧/拍 | 外部账单/拍 | 一局 20 拍 | 大头 |
|---|---|---|---|---|
| v0.1 估的 | 0.17 | $0.025 | $0.5 | 文本 |
| v0.2 实测（改动前） | **2.6 张** | **$0.23** | **$4.6** | **图像，占九成** |
| 现在（`MIDSTORY_KEYFRAMES=0`） | 0.05 | **$0.027** | **$0.55** | 文本，回到 v0.1 的量级 |

中间那一行是怎么涨上去的：硬切率 1/6 → 65%（3.2 节），再被 `PREPARE_AHEAD` 翻一倍（2.3 杠杆 5）。
它又是怎么掉回来的：**那 2.6 张图里没有一张是必需的**——它们不是省下来的，是**因为画了才产生 bug**
（3.2 节的 7/11）。v0.1 那句"GPU 是绝对瓶颈，不是 LLM"在**延迟**上一直是对的，中间那一行让它在
**钱**上错了一阵，现在又对了。

剩下要省的是文本，而 $0.55 一局已经不是一个值得优先动的数字了——**这一节从"最要紧的三件事之一"
降级成了一个记账页**。真要省，最干净的一条仍然是"只为 `predicted_choice` 预测的那一支准备孙拍"，
它把 PromptIR 的 6 次砍到 4 次；而 `PREPARE_AHEAD=0` 从此**不再是省钱旋钮**（它省下的那笔图钱没有
了，只剩可忽略的 PromptIR 调用），所以默认就该一直开着。

---

## 10. 实施路线

| 阶段 | 目标 | 状态 |
|---|---|---|
| **P0 摸底** | 跑通第 8 节测试清单 1–5 | 🟡 一半：1/2/4/9 有答案了，但是**边做边量**出来的，不是先摸底再动手；`bench/` 里的 `probe_sglang.py` / `bench_h3.py` / `bench_promptir.py` / `chain_drift.py` 还没跑 |
| **P1 骨架** | 单拍能跑：世界观 → PromptIR → H3 → 浏览器播放 | ✅ 没有 h3-wrapper，编排器直连（5.2） |
| **P2 循环** | Director + 双分支预生成 + 决断时刻 + 无缝交换 | ✅ 能从头玩到尾 |
| **P3 世界** | 世界圣经 + 三幕大纲 + flags/stats + 末帧衔接 + 重锚定 | 🟡 末帧衔接 ✅ 且可逐帧验证（3.1），现在几乎每拍都走它；**重锚定按 3.3 关掉了**，漂移仍然一次没量过 |
| **P4 产品** | 故事树回溯 + 导出电影 + 分享 + 候场室 + 全套降级 | 🟡 故事树 ✅ 历史记录 ✅ 调试面板 ✅ 降级大部分 ✅；**导出/分享/候场室没做** |
| **P5 规模** | 槽位调度器 + depth-2 投机 + 多会话 | 🟡 调度器 ✅；depth-2 试过更慢已回退（2.3 杠杆 4）；多会话没做 |

**P0 那条"硬门槛"的判断被证伪了。** 它说：PromptIR 压不到 5s 且 i2va 明显慢，就得上 depth-2。
实际是 PromptIR 压到了 4.57s，而 depth-2 在 2 槽下**反而更慢**——真正救回窗口的是
`PREPARE_AHEAD`（不占槽，只把 LLM 和图搬到前面做），这个手段 v0.1 完全没想到。

也就是说 P0 的方法论还是对的（"这个结论要在写 UI 之前拿到"），只是它想拿的那个结论是错的问题：
该问的不是"要不要更多 GPU"，而是"**哪些东西根本不需要 GPU，可以提前做掉**"。

按现在的数字排，接下来最值钱的三件事：
1. **拆 Worldsmith**（2.4）：开场 110s → 20–30s。这是玩家第一眼看到的东西，也是唯一一个数量级级别的差距。
2. **砍 Director 输出**（2.3 杠杆 2）：9.62s 是现在最大的单项，直接决定片尾停顿的 p95。
3. **跑 `chain_drift.py`**（3.3 / 第 8 节 #6）：接上一条第 10 条之后链变长、重锚定又关了，这是唯一一个
   "已知没兜住"的连续性风险；而它也可能直接证明这一节不用兜。

原来排在第 3 位的"关键帧省钱"（$0.21/拍里九成是图）**已经做完了**，而且不是靠省，是靠发现那些图
本身就是画面 bug 的来源——见头表第 10 条。

---

## 11. 内容安全 —— 只做到了一条

| | 状态 |
|---|---|
| 世界观输入过 Bedrock Guardrails（真实人物 / 未成年人 / 极端暴力具体化） | ⬜ **没做**，代码里没有 Guardrails |
| Director 输出过同一 Guardrail，防止开放叙事逐步漂移到红线 | ⬜ **没做** |
| 每个会话的世界观 + 所有 IR 落盘存档，可审计 | ✅ `DATA_DIR/sessions/<sid>/`：`session.json`、`ir/`、`events.jsonl`，调试面板可直接读 |
| 前端明确标注 AI 生成，导出视频加水印 | ⬜ **没做**（导出本身也没做，见 6.5） |

现在只有一台机器、一个人玩，风险是可控的；但这一节是**给人玩之前的硬门槛**，不是 nice-to-have。
两条 Guardrail 都在同一处接进来最省事：`llm.py` 的 `structured()` 外面包一层，因为世界观输入和
Director 输出都从那里过。开放叙事的漂移风险比一次性输入更高——玩家可以用 20 拍慢慢把故事推到
红线上，任何只检查第一次输入的做法都挡不住。

---

## 附：SGLang 请求模板（实际发出去的那个）

v0.1 的模板照抄会 **400**：`model` / `seconds` / `quality` / `num_inference_steps` 这四个字段里，
三个不该发、一个会被拒。实际发的是（`services/orchestrator/app/gpu.py` 的 `_body`）：

```jsonc
// POST http://172.31.45.68:30010/v1/videos
{
  "task": "fl2va",
  "prompt": "<PromptIR 输出的三段 IR，单 \\n 分隔>",
  "conditions": [
    // http URL，不是 file://：H3 自己来我们的 :8101 拉这张图
    { "type": "image",
      "uri": "http://172.31.42.3:8101/<sid>/kf-b3-c1f36e.png",
      "role": "keyframe", "frame_index": 0 }
  ],
  "target": {
    "short_edge": 480,
    "aspect_ratio": "16:9",
    // 14.375s = 345 帧 = 5 + 17×20。不在这个格子上的值服务端会静默取整
    "duration_seconds": 14.375
  },
  "num_outputs_per_prompt": 1,
  "seed": 0,
  "flow_shift": 12.0,        // 抄的部署参数，改了画面就变，所以是配置不是常量
  "audio_flow_shift": 3.0
  // 故意不发 num_inference_steps：步数被蒸馏固定成 8，发别的值直接报错（第 7 节）
  // 故意不发 quality / seconds / model：这个构建不认
}
```

回来的是 `{"id": "..."}`，轮询 `GET /v1/videos/{id}` 到 `status: "succeeded"`，
成片 `GET /v1/videos/{id}/content`（`url` 字段永远是 null，别等它）。

- 开场（每局一次）：`uri` 指向 `ASSETS_DIR/<sid>/kf-<beat>.png`（SD3.5 画的开场关键帧）
- **其余每一拍**：`uri` 指向上一拍的 `last.png`——同一个目录、同一个只读服务，**没有上传这一步**。
  `cut` / `timeskip` 也走这条，换地方靠 IR 里的运镜要求（3.2）
- 例外：父拍的末帧不存在，或 `MIDSTORY_KEYFRAMES=1`——那时才会再出现一张这一拍自己画的 SD3.5 图
